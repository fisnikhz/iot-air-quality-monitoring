{
  "name": "sensor-emitter",
  "version": "0.1.0",
  "private": true,
  "description": "Outdoor air-quality sensor reading simulator",
  "scripts": {
    "build": "tsc",
    "start": "npm run build --silent && node dist/index.js"
  },
  "dependencies": {
    "kafkajs": "^2.2.4"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18"
  }
}
